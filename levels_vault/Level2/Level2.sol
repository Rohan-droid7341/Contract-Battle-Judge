// SPDX-License-Identifier: MIT
pragma solidity ^0.8.19;

/**
 * @title VaultAccess (Level 2)
 * @dev Mission: Secure access to the vault.
 */
contract VaultAccess {
    
    mapping(address => bool) public hasAccess;

    // Hint: Look at instructions.md to figure out the exact entry conditions!
    
    function enter(uint256 passkey) public {
        // Build your lock mechanism here
        
    }

}
